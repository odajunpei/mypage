<div class="container">
  <div class="row">
    <div class="col-10 offset-1">
      <div class="d-flex justify-content-between">
        <h2 class="mt-3 pb-4">商品一覧</h2>
        <h4 class="mt-4">
          <%= link_to "新規登録", new_admin_application_path %>
        </h4>
      </div>
      <table class='table table-hover table-inverse'>
        <tbody>
          <% @applications.each do |application| %>
            <tr>
              <th>
                <labe
              </th>
              <td>
                <%= attachment_image_tag @application, :image, size:"200x200" %>
              </td>
              <td>
                <%= application.id %>
              </td>
              <td>
                <%= link_to admin_application_path(application) do %>
                  <%= application.name %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
   </div>
  </div>
</div>